# Terraform Core Files and Directories
# Cache directory where providers and modules are stored
.terraform/
# Stores the state of your infrastructure
*.tfstate
# Backup state file created during operations
*.tfstate.backup
# Crash log files
crash.log
# Exclude all .tfvars files, which are likely environment-specific or contain secrets.
# IMPORTANT: This includes the terraform.tfvars file you created.
*.tfvars
# Exclude override files as they are typically used for local testing or sensitive data
override.tf
override.tf.json
*_override.tf
*_override.tf.json
# Include override files you DO want to track explicitly with !override.tf

# If you save Terraform plans to files, ignore them as they can contain sensitive info
*.tfplan

# Terraform provider lock file - Typically should NOT be ignored if collaborating
# .terraform.lock.hcl  <-- Keep this line COMMENTED OUT or REMOVE it to track the lock file (Recommended)

# OS-specific files
.DS_Store
Thumbs.db

# Editor/IDE specific files (optional, add yours if needed)
.vscode/
.idea/
*.swp
*~

# Any other sensitive files specific to your project
# e.g., my-secret-key.pem